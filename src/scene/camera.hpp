#pragma once

#include "../common.h"
#include "../types.h"
#include "../debug/debug.hpp"

namespace cruelEngine {
    class Camera{
        public:
            Camera();
            virtual ~Camera();
    };
}
