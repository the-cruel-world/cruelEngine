#pragma once

#include "../common.h"
#include "../debug/debug.hpp"
#include "../types.h"

namespace cruelEngine {
class Camera {
public:
  Camera();
  virtual ~Camera();
};
} // namespace cruelEngine
