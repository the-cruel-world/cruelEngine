#include "render_context.hpp"
#include "../window/window.hpp"

namespace cruelEngine {
    
    RenderContext::RenderContext(const Window& _window) : mainWindow (_window)
    {

    }

    RenderContext::~RenderContext() {}
}