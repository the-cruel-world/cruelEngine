#pragma once

#include "vkcommon.h"

namespace cruelEngine {
namespace VulkanContext {
    class Semaphores {
    public:
        Semaphores();
        virtual ~Semaphores();

    public:

    private:

    };
}
}