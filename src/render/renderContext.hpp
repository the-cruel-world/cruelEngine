#pragma once

#include "../common.h"
#include "../debug/debug.hpp"

namespace cruelEngine {
    class RenderContext {
    public:
        RenderContext();
        virtual ~RenderContext();

    public:

    private:

    };
}